# Only needed if the frontend is served from a different origin than the
# backend (e.g. deployed separately). Leave unset for local dev -- Vite's
# dev server proxies /api to the backend automatically (see vite.config.js).
VITE_API_BASE_URL=
